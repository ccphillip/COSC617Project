<%= form_for(@privacy_level) do |f| %>
  <% if @privacy_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@privacy_level.errors.count, "error") %> prohibited this privacy level from being saved:</h2>

      <ul>
      <% @privacy_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br />
    <%= f.number_field :level %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
